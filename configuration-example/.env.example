# Optional: choose a non-default configuration file.
GESTALTX_CONFIG=gestaltx.config.yaml

# ollama (default) or openrouter
GESTALTX_LLM_PROVIDER=ollama

# Required only when using OpenRouter.
OPENROUTER_API_KEY=
